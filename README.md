# Mock Score API

Simple mock API to test Chainlink Functions or other oracle systems.

## Endpoints

### GET /
Health check

### GET /score
Returns a deterministic score

Response:
```json
{ "score": 87 }
